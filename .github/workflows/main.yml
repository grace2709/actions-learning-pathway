name: hello-world
on: push
jobs:
  hello-world-job:  
    runs-on: ubuntu-latest
    steps:
    - name: Check ot repository code
      uses: actions/checkout@v3
    - run: echp "$(cat hello_world.txt)"
  
